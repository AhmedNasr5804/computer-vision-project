# SavedModel Variables Description

This folder stores the variable tensors associated with `saved_model.pb`.

Typical files include TensorFlow variable shards and index data required to fully load the SavedModel.
